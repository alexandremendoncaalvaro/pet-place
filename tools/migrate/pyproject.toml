[project]
name = "pet-place-migrate"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "firebase-admin>=7.1.0",
  "google-cloud-firestore>=2.21.0",
  "google-cloud-storage>=3.6.0",
  "requests>=2.32.0",
]

[tool.uv]
package = false
