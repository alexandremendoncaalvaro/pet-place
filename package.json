{
  "name": "pet-place",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "dev:worker": "wrangler dev",
    "build": "vite build",
    "deploy": "npm run deploy:prod",
    "deploy:prod": "npm run build && wrangler deploy --env production && npm run pages:prod",
    "deploy:dev": "npm run build && wrangler deploy --env dev && npm run pages:dev",
    "cf:migrate": "npm run cf:migrate:prod",
    "cf:migrate:prod": "wrangler d1 migrations apply DB --env production --remote",
    "cf:migrate:dev": "wrangler d1 migrations apply DB --env dev --remote",
    "pages:prod": "wrangler pages deploy pages-proxy --project-name pet-place --branch main",
    "pages:dev": "wrangler pages deploy pages-proxy-dev --project-name pet-place-dev --branch development",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "security:secrets": "node tools/security/scan-secrets.mjs",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "quality": "npm run security:secrets && npm run lint && npm test && npm run build",
    "smoke:dev": "node tools/smoke.mjs https://pet-place-dev.pages.dev",
    "smoke:prod": "node tools/smoke.mjs https://pet-place.pages.dev"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^6.0.1",
    "browser-image-compression": "^2.0.2",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "lucide-react": "^1.14.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "tailwind-merge": "^3.5.0",
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260430.0",
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "eslint": "^10.3.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "wrangler": "^4.87.0"
  }
}
